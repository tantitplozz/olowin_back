# Web Framework
fastapi>=0.115.0
uvicorn>=0.34.0
pydantic>=2.0.0
starlette>=0.40.0
jinja2>=3.1.2

# Database
pymongo>=4.5.0
motor>=3.3.0
chromadb>=0.4.18

# AI & Language Models
metagpt==0.8.2 # Locked for compatibility
langchain==0.0.231 # Locked for MetaGPT compatibility
langgraph>=0.0.20
python-jose[cryptography]>=3.3.0

# Utilities
python-dotenv>=1.0.0
loguru==0.6.0 # Locked for MetaGPT compatibility
slowapi>=0.1.8
httpx>=0.24.1
requests # Added as per user suggestion

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.1

# Browser Automation
playwright>=1.40.0

# Development (Consider moving these to a requirements-dev.txt or optional dependencies in pyproject.toml)
black>=23.7.0
flake8>=6.1.0
mypy>=1.5.1

# Add other specific dependencies like a specific browser automation library if needed by agents
ollama
tenacity # For retry logic
mongomock # For mocking MongoDB in tests
ruff # For linting and formatting
# accelerate # Often needed for distributed training / efficient use of resources with transformers 